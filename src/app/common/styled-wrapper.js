import React from 'react';
import styled from 'styled-components';

export const ContentWrapper = styled.div`
    height: inherit;
    margin-left: auto;
    margin-right: auto;
    min-height: inherit;
    max-width: 960px;
    padding: 13px;
`
