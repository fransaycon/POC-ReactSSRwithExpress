import React from 'react';
import styled from 'styled-components';

export const ContentWrapper = styled.div`
    height: inherit;
    margin-left: auto;
    margin-right: auto;
    max-width: 960px;
`